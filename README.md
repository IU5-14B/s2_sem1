# Bank System Кейс 2
Консольное ООП-приложение на C++, демонстрирующее простую банковскую систему (кейс 2).
## Сборка и запуск
```bash
rm -rf build && cmake -B build && cmake --build build && ./build/bank_app
```

## Архитектура

- `BankAccount` — хранит номер счета, владельца и баланс; поддерживает операции пополнения/снятия и печать выписки.
- `Bank` — управляет коллекцией счетов, создает новые счета, ищет по номеру и считает общий баланс.

## AI log

- Использовался Codex/ChatGPT для генерации каркаса проекта и проверки требований.
- Идеи по структуре файлов и CMake были получены с помощью ИИ.
- Итоговый код и README были проверены вручную.
